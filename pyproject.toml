[project]
name = "fdmodel"
version = "0.1.0"
description = "Phase 1 periodic vorticity model utilities"
requires-python = ">=3.10"
dependencies = ["numpy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
